{
  "name": "scaffold-toro-app",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm workspace core dev",
    "build": "pnpm workspace core build",
    "start": "pnpm workspace core start",

    "core:dev": "pnpm workspace core dev",
    "core:build": "pnpm workspace core build",
    "core:start": "pnpm workspace core start",
    "core:lint": "pnpm workspace core lint",
    "core:format": "pnpm workspace core format",

    "compile": "pnpm workspace contracts compile",
    "deploy": "pnpm workspace contracts deploy",

    "contracts:compile": "pnpm workspace contracts compile",
    "contracts:deploy": "pnpm workspace contracts deploy",
    "contracts:lint": "pnpm workspace contracts lint",
    "contracts:format": "pnpm workspace contracts format",
    "contracts:test": "pnpm workspace contracts test",

    "lint": "pnpm workspace core lint && pnpm workspace contracts lint",
    "format": "pnpm workspace core format && pnpm workspace contracts format",
    "test": "pnpm workspace core test && pnpm workspace contracts test"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=20.18.3"
  }
}
